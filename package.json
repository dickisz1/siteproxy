{
  "name": "siteproxy",
  "version": "1.0.0",
  "main": "api/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "wrangler-login": "wrangler login",
    "deploy-cf-worker": "wrangler deploy --config wrangler.worker.jsonc",
    "deploy-cf-page": "wrangler pages deploy"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/node-server": "^1.8.0"
  },
  "devDependencies": {
    "wrangler": "^4.25.1"
  }
}
